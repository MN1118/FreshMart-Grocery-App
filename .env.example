# Base URL for the Flask API, once you wire it up (see README > Connecting the Flask backend).
# Unused for now — the app runs entirely on mock data in src/data/products.js.
VITE_API_BASE_URL=http://localhost:5000
